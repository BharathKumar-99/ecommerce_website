import 'package:flutter/cupertino.dart';

class VendersPage extends StatelessWidget {
  const VendersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
